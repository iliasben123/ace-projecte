import 'package:flutter/material.dart';

class MedicalReportsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rapports Médicaux'),
      ),
      body: Center(
        child: Text('Interface pour les rapports médicaux.'),
      ),
    );
  }
}
